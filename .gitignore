# Hugo build output & caches
/public/
/resources/_gen/
.hugo_cache/
.hugo_build.lock
.wrangler/

# Node (esbuild/katex/mathlive/pagefind land here when installed)
/node_modules/
/playwright-report/
/test-results/
.env*

# OS cruft
.DS_Store

# IDE-local project state
.idea/

# Local upstream-errata scratchpad (submitted to OpenStax, not published here)
/docs/openstax-errata.md

# Local source textbooks (reference material, not site assets)
/sources/
/.mcp.json
/.claude/

# Transient grader-differential harness (old/new module copies for A/B grading)
/.tmp-grader-diff/

# Scratch space for review sessions: working scripts and pre-change tarballs.
# Ignored rather than trusted to be tidied — it was reported as `?? _to_delete/`
# with a 207 KiB backup archive inside, one `git add -A` away from being
# committed. Nothing here is a build input; delete the directory whenever.
/_to_delete/
/docs/accounts-plan.md
