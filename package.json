{
  "name": "athenaeum-populi-hugo",
  "private": true,
  "license": "MIT",
  "description": "Hugo build of Athenaeum Populi. Keep the dependency list deliberately small and document any new runtime dependency in the README and lockfile.",
  "scripts": {
    "build": "npm run build:hugo && npm run search:index",
    "build:hugo": "hugo --gc --minify --cleanDestinationDir --cacheDir \"$(pwd)/.hugo_cache\"",
    "search:index": "pagefind --site public --root-selector 'main#content' --exclude-selectors '.katex,svg,template,.ap-mc-options,.ap-mc-feedback'",
    "check:routes": "node tools/check-route-parity.mjs",
    "check:links": "node tools/check-links.mjs",
    "check:search": "node tools/check-pagefind.mjs",
    "audit:build": "node tools/audit-build.mjs",
    "check:build": "npm run check:routes && npm run check:links && npm run check:search && npm run audit:build",
    "test:a11y": "playwright test tests/accessibility.spec.mjs",
    "test:browser": "playwright test",
    "ci": "npm test && npm run verify:replay && npm run build && npm run check:build && PLAYWRIGHT_SKIP_BUILD=1 npm run test:browser",
    "serve:public": "node tools/serve-public.mjs",
    "preview": "npm run build:hugo && npm run search:index -- --serve",
    "serve": "hugo server --cacheDir \"$(pwd)/.hugo_cache\"",
    "test": "node --test && npm run validate && npm run verify:content && npm run verify:answers && npm run verify:ledger && npm run lint && npm run check:figures",
    "check:figures": "node tools/check-figure-overlaps.mjs content",
    "lint": "node tools/lint-all.mjs content",
    "validate": "node tools/validate-content.mjs content 0",
    "verify:content": "node tools/verify-all.mjs content",
    "verify:answers": "node tools/verify-answers.mjs content --min-verified 3043",
    "verify:replay": "node tools/verify-replay.mjs content --min-replayed 6203",
    "verify:ledger": "node tools/answer-ledger.mjs check content --min-exercises 6999 --max-unverifiable 0",
    "ledger:stats": "node tools/answer-ledger.mjs stats content",
    "ledger:list": "node tools/answer-ledger.mjs list content",
    "ledger:merge": "node tools/answer-ledger.mjs merge",
    "verify-section": "node tools/verify-section.mjs",
    "baseline:update": "node tools/update-baselines.mjs content",
    "source:fetch": "node tools/openstax-source.mjs fetch",
    "source:verify": "node tools/openstax-source.mjs verify-map",
    "source:check": "node tools/openstax-source.mjs audit",
    "source:history": "node tools/openstax-source.mjs history",
    "source:status": "node tools/openstax-source.mjs status",
    "test:e2e": "playwright test tests/browser-check.spec.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "//search": "search:index excludes rendered math (.katex) from the index entirely: flattening the visual KaTeX DOM turns stacked fractions into mathematically false digit strings (2/3 became 32 in excerpts), and the MathML layer would surface raw token noise instead. Excerpts skip equations rather than corrupt them. .ap-mc-options / .ap-mc-feedback are multiple-choice control noise, not content.",
  "//deps": "esbuild is NOT listed: Hugo Pipes js.Build embeds esbuild. katex/mathlive/pagefind are pinned: KaTeX assets are vendored for offline builds, MathLive supplies math input, and Pagefind builds one global site-wide search index.",
  "//versions": "KaTeX 0.16.22 matches the engine embedded in Hugo 0.164.0; its CSS must match Hugo's generated HTML. MathLive 0.110.0 and @cortex-js/compute-engine 0.58.0 are pinned because grading behavior is version-sensitive. Upgrade these versions only as a coordinated, fully verified architecture change.",
  "dependencies": {
    "@cortex-js/compute-engine": "0.58.0",
    "katex": "0.16.22",
    "mathlive": "0.110.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.1",
    "@playwright/test": "1.58.2",
    "pagefind": "1.5.2"
  }
}
